{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "nopagination-txt",
            "type": "shell",
            "command": "make nopagination-txt",
            "group": "build",
            "options": {
                "cwd": "/workspaces/${workspaceFolderBasename}${pathSeparator}src"
            }
        },
        {
            "label": "all",
            "type": "shell",
            "command": "make all",
            "group": "build",
            "options": {
                "cwd": "/workspaces/${workspaceFolderBasename}${pathSeparator}src"
            }
        }
    ]
}